# Curriculum Map — lessons ⇄ subjects ⇄ exercises

> **Purpose.** One canonical index for the whole Rust course: every lesson's subject, what it teaches, and which exercise (if any) practices it — plus the reverse. Use it to sequence exercises against lessons, spot gaps, and author or adjust lessons without re-reading the whole corpus each time.

> **Source of truth.** Lesson order = the `NN[x]-` file prefixes under `lessons/`. Exercise→lesson mapping = `CONCEPT_LESSON` in `gui/index.html` (keyed by each exercise's `concept`). Subjects live in `docs/curriculum-subjects.json` (editable). **Generated by `scripts/build-curriculum-map.py` — do not hand-edit; re-run it.**

> **Totals.** 78 lessons · 91 exercises · 20 lessons with no exercise yet.


---


## 1 · Lessons in learning order — subject → exercises

Each lesson, in the order the app presents it, with its subject and the exercise(s) that practice it.


| # | Lesson | Subject | Practiced by |
|---|--------|---------|--------------|
| 0 | **Lesson 0 — Hello, World (your first program)**<br>`00-hello-world` | Teaches the skeleton of every Rust program: an entry point that runs and prints text to the screen. | _— none yet_ |
| 1 | **Lesson 1 — Bindings & Immutability**<br>`01-bindings-and-immutability` | Teaches that naming a value with let binds it immutably by default so it can't be reassigned later. | `basics/08_use_before_init` |
| 2 | **Lesson 2 — Mutability (`mut`)**<br>`02-mutability` | Teaches how mut opts a binding into being changeable while keeping its type fixed. | `basics/01_immutable_assign` |
| 3 | **Lesson 3 — Shadowing**<br>`03-shadowing` | Teaches shadowing: a second let with the same name makes a new binding that can change the value's type, unlike mut. | `basics/09_shadow_type_change` |
| 4 | **Lesson 4 — Constants (`const`)**<br>`04-constants` | Teaches const for values fixed forever and known at compile time, requiring an explicit type. | `basics/10_const_needs_type` |
| 5 | **Lesson 5 — The scalar types: integers, floats, `bool` & `char`**<br>`05-scalar-types` | Teaches Rust's four scalar types and that Rust never converts between number types silently. | `basics/02_annotation_needed`, `basics/04_no_coercion` |
| 6 | **Lesson 5b — Integer overflow: your first *runtime* panic**<br>`05b-integer-overflow` | Teaches that arithmetic overflowing an integer's width causes a runtime panic in debug builds rather than silent wrapping. | `basics/05_integer_overflow`, `basics/05b_divide_by_zero`, `basics/05c_subtract_overflow` |
| 7 | **Lesson 6 — Expressions, statements & the semicolon**<br>`06-expressions-statements-semicolon` | Teaches the difference between expressions (produce values) and statements, and how a semicolon discards a value to produce unit (). | `basics/11_semicolon_returns_unit` |
| 8 | **Lesson 7 — Functions**<br>`07-functions` | Teaches declaring functions with typed parameters and a return type, returning via the last expression. | `basics/03_fn_arg_type`, `basics/07_fn_arg_count`, `basics/07b_duplicate_fn_name` |
| 9 | **Lesson 8 — Comments & printing**<br>`08-comments-and-printing` | Teaches comment syntax and the full range of printing with println!, format!, and Display vs Debug formatting. | `basics/13_display_vs_debug` |
| 10 | **Lesson 9 — `if` / `else if` / `else`: making the program choose**<br>`09-if-else` | Teaches branching: how to run one block of code when a condition is true and another otherwise, using if/else if/else with real bool conditions (no truthiness). | `control-flow/06_bool_condition` |
| 11 | **Lesson 9b — `if` is an expression: branching that produces a value**<br>`09b-if-as-expression` | Teaches that an if/else is an expression that produces a value, so it can sit on the right of a let, with the requirement that every arm produce the same type. | `control-flow/01_if_expr` |
| 12 | **Lesson 10 — `loop`: repeat until you `break`**<br>`10-loop-and-break` | Teaches the loop keyword that repeats forever until break, and its unique power to carry a value out via break <value> because it has exactly one exit. | `control-flow/03_loop_break_value` |
| 13 | **Lesson 10b — `while`: repeat while a condition holds**<br>`10b-while` | Teaches the while loop that repeats while a bool condition holds, checking the condition each pass and stopping when it becomes false. | `control-flow/07_while_condition` |
| 14 | **Lesson 10c — `for` & ranges: walk each item**<br>`10c-for-and-ranges` | Teaches the for loop that walks each item of a collection or range without managing an index, so it cannot run off the end. | `control-flow/04_for_scope` |
| 15 | **Lesson 11 — `match` (intro)**<br>`11-match-intro` | Teaches match: hand it one value, list cases as patterns, and it runs the first arm that fits, with compiler-enforced exhaustiveness. | `control-flow/02_match_exhaustive` |
| 16 | **Lesson 12 — `String` vs `&str`**<br>`12-string-vs-str` | Teaches the difference between a borrowed fixed &str view and an owned growable String, and how to build and grow a String. | `collections/02_string_vs_str`, `collections/09_return_owned_string` |
| 17 | **Lesson 13 — Tuples: grouping mixed values**<br>`13-tuples` | Teaches tuples: bundling a fixed number of values of different types into one value, accessed by destructuring or by position. | `collections/03b_tuple_field` |
| 18 | **Lesson 13b — Arrays: a fixed row of one type**<br>`13b-arrays` | Teaches arrays: a fixed-length row of same-type values whose length is part of the type [T; N], with bounds checks caught at compile time or run time. | `collections/06b_array_length` |
| 19 | **Lesson 13c — Slices: a borrowed window**<br>`13c-slices` | Teaches slices: a borrowed window &[T] into part of a sequence whose type erases the length, so one function accepts any size. | `collections/12_slice_borrow` |
| 20 | **Lesson 14 — `Vec`: the growable list**<br>`14-vec` | Teaches Vec<T>, the growable list whose length lives at run time, and the deliberate choice between indexing (which panics on a bad index) and .get() (which returns an Option). | `collections/01_vec_push_type`, `collections/06_index_out_of_bounds` |
| 21 | **Lesson 14b — `HashMap`: the lookup table**<br>`14b-hashmap` | Teaches HashMap<K,V> as a key-to-value lookup table, including the required import, the entry/or_insert counting idiom, and its unordered nature. | `collections/05_hashmap_insert` |
| 22 | **Lesson 15 — Ownership, scope & drop**<br>`15-ownership` | Teaches Rust's ownership model: every value has one owner, and when the owner goes out of scope the value is dropped and freed automatically at build-time-proven safety. | `ownership/10_explicit_drop_call` |
| 23 | **Lesson 15b — Moves: assignment hands ownership over**<br>`15b-moves` | Teaches that assigning or passing a String moves ownership (invalidating the old name) so a value is never freed twice, and using the moved value is Rust's most famous compile error. | `collections/08_use_after_move_into_fn`, `ownership/01_move` |
| 24 | **Lesson 15c — Copy & Clone: when assignment duplicates**<br>`15c-copy-and-clone` | Teaches the Copy trait (why small stack values duplicate on assignment instead of moving) versus non-Copy types that move, and .clone() as the explicit, visible deep copy. | `ownership/09_clone_string` |
| 25 | **Lesson 16 — Shared references: borrow to *read***<br>`16-shared-references` | Teaches shared references (&T) that let code borrow a value to read it without taking ownership, so the original owner keeps the value and can have many simultaneous read-only borrows. | `ownership/02b_deref_non_reference`, `ownership/04_move_out_of_borrow` |
| 26 | **Lesson 16b — Mutable references & the borrowing rules**<br>`16b-mutable-references` | Teaches mutable references (&mut T) for borrowing to change a value, plus the two borrowing rules (shared xor mutable, references must stay valid) enforced by the borrow checker. | `basics/06_mutable_method_needs_mut`, `collections/07_fill_vec_needs_mut`, `ownership/02_borrow`, `ownership/03_dangling`, `ownership/05_double_mut_borrow`, `ownership/06_assign_while_borrowed`, `ownership/07_move_while_borrowed` |
| 27 | **Lesson 17 — Slices in Depth**<br>`17-slices-in-depth` | Teaches that a slice is a borrow pointing into a collection, so while the slice is alive the collection is borrowed immutably — which turns stale-index bugs into compile errors. | `collections/13_slice_holds_borrow` |
| 28 | **Lesson 18 — Structs: bundle your data**<br>`18-defining-structs` | Teaches defining and instantiating structs to group related fields into one named owned type, including field-init shorthand, struct update, tuple structs, and unit structs. | `types/01_struct_build`, `types/01b_struct_extra_field`, `types/02_field_privacy` |
| 29 | **Lesson 18b — Methods & `impl` blocks**<br>`18b-methods-and-impl` | Teaches attaching behaviour to structs via impl blocks and methods, where the receiver (&self / &mut self / self) mirrors the Phase-4 borrow rules, plus associated functions called with ::. | `types/04_struct_method` |
| 30 | **Lesson 18c — Printing your own types: `#[derive(Debug)]`**<br>`18c-derive-debug` | Adding `#[derive(Debug)]` above a struct makes it printable with `{:?}` (compact) and `{:#?}` (pretty), and the compiler literally tells you to add that line when it's missing. | `types/08_derive_debug` |
| 31 | **Lesson 19 — Enums: one-of types**<br>`19-enums` | An enum models a value that is one of several shapes, with variants that can be empty or carry data, and you attach methods with impl that match on self. | `control-flow/05_match_enum_data` |
| 32 | **Lesson 19b — `Option<T>`: Rust has no null**<br>`19b-option` | Rust has no null; instead the possibility of a missing value lives in the type `Option<T>` (Some/None), so the compiler forces you to handle absence. | `types/03_option_value` |
| 33 | **Lesson 19c — `match` patterns in depth**<br>`19c-match-in-depth` | The full match pattern vocabulary — binding inner data, multiple patterns, ranges, guards, and catch-alls — while the compiler still enforces exhaustiveness. | `control-flow/08_or_pattern_binding` |
| 34 | **Lesson 19d — Concise matching: `if let`, `while let`, `let…else`**<br>`19d-concise-matching` | Three short forms — `if let`, `while let`, and `let…else` — handle a single case by deliberately trading away exhaustiveness checking to keep the happy path flat. | `types/05_refutable_let` |
| 35 | **Lesson 20 — `Result<T, E>`: errors are values**<br>`20-result` | In Rust a recoverable error is an ordinary value carried in the return type as `Result<T, E>` (Ok/Err), which you read with match or unwrap_or rather than hidden exceptions. | `error-handling/01_result_is_not_t` |
| 36 | **Lesson 20b — `panic!`, `unwrap` and `expect`: the blunt path**<br>`20b-panic-unwrap-expect` | The blunt error path stops the program on the spot: `panic!` halts the thread, and `unwrap`/`expect` give the Ok/Some value or panic — a runtime crash, not a compile error. | `types/05_unwrap_none`, `error-handling/03_unwrap_err_panics` |
| 37 | **Lesson 20c — The `?` operator: pass the problem up**<br>`20c-question-mark` | The `?` operator is a whole error-handling match in one character: on Err it early-returns the error from the enclosing function, and on Ok it unwraps and continues. | `error-handling/02_question_mark_propagates`, `error-handling/04_custom_error_from`, `error-handling/05_question_mark_option_in_result` |
| 38 | **Lesson 21 — Packages & crates: what you've been building all along**<br>`21-packages-and-crates` | Names the units of a Rust project: a crate is the smallest unit the compiler builds (binary with src/main.rs or library with src/lib.rs) and a package is a Cargo.toml plus one or more crates. | _— none yet_ |
| 39 | **Lesson 21b — Modules & the module tree**<br>`21b-modules` | A module (`mod`) is a named group inside a crate that provides organization and privacy, and nested modules form a tree rooted at `crate` addressed by `::` paths. | _— none yet_ |
| 40 | **Lesson 21c — Splitting modules into files**<br>`21c-modules-in-files` | It teaches how a `mod name;` declaration (semicolon, no body) tells the compiler to load a module's body from a separate file so the directory layout mirrors the module tree. | _— none yet_ |
| 41 | **Lesson 22 — Paths: naming items across modules**<br>`22-paths` | It teaches how to name an item in another module using `::`-separated paths, distinguishing absolute (crate::) from relative (self::/super::/sibling) forms. | `modules/04_super_path` |
| 42 | **Lesson 22b — Privacy & `pub`: private by default**<br>`22b-privacy-and-pub` | It teaches that every item is private to code outside its module by default and that `pub` opens one specific item, with the trap that `pub mod` does not make the module's contents public. | `modules/01_private_module`, `modules/02_private_fn` |
| 43 | **Lesson 22c — `pub` on structs & enums: an asymmetry**<br>`22c-pub-structs-enums` | It teaches the asymmetry that a `pub struct` keeps its fields private (requiring a public constructor) while a `pub enum` makes all its variants public. | _— none yet_ |
| 44 | **Lesson 23 — The `use` Keyword**<br>`23-the-use-keyword` | It teaches how the `use` keyword brings a path into scope once so code can call items by their short name, plus handling name clashes with `as` and the scope-locality trap. | `modules/03_use_scope`, `modules/05_unresolved_import` |
| 45 | **Lesson 24 — Generic functions: the `<T>` placeholder**<br>`24-generic-functions` | It teaches how a generic type parameter `<T>` lets one function work for many types by leaving a type hole filled at each call, ending copy-paste-and-rename duplication. | `generics/01_bound` |
| 46 | **Lesson 24b — Generic structs, enums & methods**<br>`24b-generic-types` | It teaches how to put the `<T>` placeholder into struct definitions, enum definitions, and impl blocks so one type definition serves every element type. | `generics/02_generic_struct_same_type` |
| 47 | **Lesson 25 — Traits: declare and implement**<br>`25-traits-declare-implement` | It teaches how to declare a trait as a set of method signatures and implement it for a type with `impl Trait for Type`, distinguishing required methods from default methods. | `traits/01_display_bound`, `traits/02_trait_in_scope`, `traits/04_missing_method`, `advanced/04_operator_overload_add` |
| 48 | **Lesson 25b — Trait bounds: demanding behaviour from a generic `T`**<br>`25b-trait-bounds` | It teaches how a trait bound constrains a generic `T` to types implementing a trait so the body may use the trait's behaviour, closing Lesson 24's E0369 wall. | `advanced/02_orphan_rule_newtype`, `advanced/05_supertrait_display` |
| 49 | **Lesson 26 — Lifetime annotations: the `longest` function**<br>`26-lifetime-annotations` | It teaches lifetime annotations (`'a`) as generics for how long references stay valid, needed when a function returns one of several input references so the compiler knows which it borrows from. | `lifetimes/03_borrow_outlives_value`, `lifetimes/04_temporary_dropped`, `lifetimes/01_longest` |
| 50 | **Lesson 26b — Lifetime elision: the annotations you never write**<br>`26b-lifetime-elision` | It explains the compiler's lifetime elision rules — chiefly that a function with one input reference donates that lifetime to all outputs — so you know which functions demand a manual annotation and which don't. | _— none yet_ |
| 51 | **Lesson 26c — Lifetimes in structs: closing the Lesson 18 promise**<br>`26c-lifetimes-in-structs` | It teaches how to declare a lifetime on a struct so a field can hold a borrowed reference (&'a str) without owning it, guaranteeing the struct never outlives the data it borrows. | `lifetimes/02_struct_lifetime` |
| 52 | **Lesson 27 — Closures: unnamed inline functions**<br>`27-closure-syntax` | It introduces closures as anonymous inline functions written with \|param\| bars, saved in variables or passed to methods, whose parameter/return types are inferred and locked on first use. | `closures/01_closure_type_lock` |
| 53 | **Lesson 27b — Closure capture & `move`**<br>`27b-closure-capture` | It teaches how closures capture surrounding variables in one of three ways (immutable borrow, mutable borrow, or take ownership), with the body deciding the mode and the move keyword forcing ownership. | `closures/02_fn_once_move` |
| 54 | **Lesson 28 — Iterators: the `next()` cursor**<br>`28-iterator-cursor` | It reveals that a for loop is driven by an iterator — a cursor implementing the Iterator trait whose next() returns Some/None — and that iter/iter_mut/into_iter choose read, mutate, or take-ownership access. | `collections/04_vec_moved`, `iterators/02_into_iter_moves` |
| 55 | **Lesson 28b — Adapter chains: `map`, `filter`, `collect`**<br>`28b-adapter-chains` | It teaches building readable iterator pipelines from lazy adapters (map, filter) plus a consumer (collect, sum) that actually drives the work, and why a chain with no consumer does nothing. | `collections/03_iter_collect`, `iterators/01_collect_annotation`, `iterators/03_sum_annotation` |
| 56 | **Lesson 29 — `Box<T>`: values on the heap**<br>`29-box` | It introduces Box<T> as a smart pointer that stores a value on the heap behind a fixed-size pointer, chiefly to give recursive self-containing types a known size via indirection. | `smart-pointers/01_recursive_box` |
| 57 | **Lesson 29b — `Deref` & `Drop`: the traits underneath**<br>`29b-deref-drop` | It explains the two traits behind smart pointers: Deref (why * follows a box like a reference) and Drop (an automatic cleanup hook that runs at scope-end in reverse creation order). | _— none yet_ |
| 58 | **Lesson 29c — `Rc<T>`: one value, many owners**<br>`29c-rc` | It teaches Rc<T> as a reference-counted smart pointer enabling multiple shared read-only owners of one heap value, cleaned up only when the count reaches zero. | `smart-pointers/02_shared_ownership_rc` |
| 59 | **Lesson 29d — `RefCell<T>`: interior mutability**<br>`29d-refcell` | It teaches RefCell<T> as interior mutability — mutating a value through a shared, immutable-looking handle by moving the borrow check to runtime, panicking on violation, and combining with Rc as Rc<RefCell<T>>. | `smart-pointers/03_refcell_runtime_borrow` |
| 60 | **Lesson 30 — Spawning threads: `spawn`, `join` & `move`**<br>`30-spawning-threads` | How to start a second line of work with a thread and safely hand it data, using thread::spawn to run a closure, join to wait for it, and move to give the thread ownership so borrows can't dangle. | `concurrency/01_spawn_move` |
| 61 | **Lesson 30b — Channels: passing data between threads**<br>`30b-channels` | How threads cooperate without sharing by sending values through a one-way channel, where send moves ownership so the sender can no longer touch the value. | `concurrency/04_channel_send` |
| 62 | **Lesson 30c — Shared state: `Arc` & `Mutex`**<br>`30c-shared-state` | How multiple threads safely share and mutate one value using Arc for many owners and Mutex for exclusive access, with the compiler's errors steering you to the Arc<Mutex<T>> design. | `concurrency/02_rc_not_send`, `concurrency/03_arc_mutex` |
| 63 | **Lesson 31 — Async syntax: `async fn`, `.await` & async blocks**<br>`31-async-syntax` | The shape of async code: async fn returns a future rather than its value, async blocks are future-valued expressions, and .await runs a future to completion inside async contexts. | _— none yet_ |
| 64 | **Lesson 31b — Futures are lazy: why async needs a runtime**<br>`31b-futures-are-lazy` | The core async rule that a future does nothing until driven, which is why async code needs a runtime and why .await is restricted and main cannot be async on its own. | _— none yet_ |
| 65 | **Lesson 32 — Trait objects: one collection, many types**<br>`32-trait-objects` | How dyn Trait behind a pointer lets one collection or function hold many different concrete types and call their shared method via runtime dynamic dispatch. | `traits/03_trait_object_dyn`, `advanced/03_unsized_str` |
| 66 | **Lesson 32b — Encapsulation: private fields, public methods**<br>`32b-encapsulation` | How keeping struct fields private and exposing only public methods enforces invariants in one place and lets you change internals without breaking callers. | _— none yet_ |
| 67 | **Lesson 32c — States as types: broken states won't compile**<br>`32c-states-as-types` | How giving each stage its own type (e.g. DraftPost vs PublishedPost) makes wrong-state operations unrepresentable, with transitions that consume self to retire the old state. | _— none yet_ |
| 68 | **Lesson 33 — Refutability: when a pattern can fail**<br>`33-refutability` | The refutability rule that governs where patterns may sit: irrefutable patterns (always match) are needed for let/for/params, while refutable ones (can fail) require an else path via if let/while let/match. | _— none yet_ |
| 69 | **Lesson 33b — Guards, `@` bindings & nested patterns**<br>`33b-guards-bindings-nested` | Three pattern power tools for match: match guards add a value condition to an arm, @ bindings capture a value while range-testing it, and nested destructuring reaches into layered data. | _— none yet_ |
| 70 | **Lesson 34 — `unsafe`: a small, audited escape hatch**<br>`34-unsafe` | It teaches that `unsafe` is a small, auditable block that unlocks five compiler-unverifiable abilities (chiefly dereferencing raw pointers) without disabling the borrow checker. | `advanced/01_unsafe_deref` |
| 71 | **Lesson 34b — Operator overloading & associated types**<br>`34b-operator-overloading` | It teaches operator overloading by implementing std::ops traits like Add on your own type, and explains associated types (type Output) so you can read forms like Future<Output = u32>. | _— none yet_ |
| 72 | **Lesson 34c — Declarative macros: `macro_rules!`**<br>`34c-macro-rules` | It teaches declarative macros with macro_rules! — pattern-to-template rules that generate code before compilation — by building a stripped-down my_vec! macro. | _— none yet_ |
| 73 | **Lesson 35 — Capstone I: a single-threaded web server**<br>`35-single-threaded-server` | It builds a real single-threaded HTTP server with TcpListener/TcpStream so the learner feels why handling connections inline (one at a time) stalls behind a slow request. | _— none yet_ |
| 74 | **Lesson 35b — Capstone II: the thread pool & graceful shutdown**<br>`35b-thread-pool` | It fixes the single-threaded server by building a fixed-size thread pool with workers pulling boxed closure jobs from a shared channel, plus graceful shutdown via Drop and join. | _— none yet_ |
| 75 | **Lesson 36 — Automated Tests**<br>`36-automated-tests` | It teaches writing automated tests that pass when they don't panic, using #[test] functions, assertion macros, and #[should_panic], run by cargo test. | _— none yet_ |
| 76 | **Lesson 37 — Documenting Rust: doc comments & doc-tests**<br>`37-doc-comments` | It teaches Markdown doc comments (/// and //!) that cargo doc renders into an HTML manual, and doc-tests — code examples that cargo test compiles and runs so docs can't drift from code. | _— none yet_ |
| 77 | **Lesson 37b — Shipping with Cargo: profiles, publishing & workspaces**<br>`37b-shipping-with-cargo` | It surveys Cargo's shipping toolkit: release vs dev build profiles, publishing crates to crates.io with required metadata, workspaces of related crates, and cargo install. | _— none yet_ |

---


## 2 · Exercises in current order → lesson (alignment check)

Exercises in the order the app serves them (`NN-phase/NN_name` file order). The **L#** is the learning-order index of the lesson each one points at. **⬅ back** flags where the lesson index drops below the previous exercise's — the learner is bounced to an *earlier* lesson than the one before. A perfectly aligned course would have **no** back-jumps.


| # | Exercise | Concept | Err | → Lesson | L# | |
|---|----------|---------|-----|----------|----|--|
| 1 | `basics/08_use_before_init` | binding | E0381 | `01-bindings-and-immutability` | 1 |  |
| 2 | `basics/01_immutable_assign` | mutability | E0384 | `02-mutability` | 2 |  |
| 3 | `basics/09_shadow_type_change` | shadowing | E0308 | `03-shadowing` | 3 |  |
| 4 | `basics/10_const_needs_type` | constant | — | `04-constants` | 4 |  |
| 5 | `basics/02_annotation_needed` | type-inference-limits | E0284 | `05-scalar-types` | 5 |  |
| 6 | `basics/04_no_coercion` | explicit-numeric-conversion | E0308 | `05-scalar-types` | 5 |  |
| 7 | `basics/05_integer_overflow` | integer-overflow | — | `05b-integer-overflow` | 6 |  |
| 8 | `basics/05b_divide_by_zero` | divide-by-zero | — | `05b-integer-overflow` | 6 |  |
| 9 | `basics/05c_subtract_overflow` | integer-overflow | — | `05b-integer-overflow` | 6 |  |
| 10 | `basics/11_semicolon_returns_unit` | expression-statement | E0308 | `06-expressions-statements-semicolon` | 7 |  |
| 11 | `basics/03_fn_arg_type` | function-arg-types | E0308 | `07-functions` | 8 |  |
| 12 | `basics/07_fn_arg_count` | function-argument-count | E0061 | `07-functions` | 8 |  |
| 13 | `basics/07b_duplicate_fn_name` | duplicate-definition | E0428 | `07-functions` | 8 |  |
| 14 | `basics/13_display_vs_debug` | display | E0277 | `08-comments-and-printing` | 9 |  |
| 15 | `basics/06_mutable_method_needs_mut` | mutable-borrow-needs-mut | E0596 | `16b-mutable-references` | 26 |  |
| 16 | `control-flow/06_bool_condition` | boolean | E0308 | `09-if-else` | 10 | ⬅ **back** |
| 17 | `control-flow/01_if_expr` | if-as-expression | E0317 | `09b-if-as-expression` | 11 |  |
| 18 | `control-flow/03_loop_break_value` | loop-break-value | E0308 | `10-loop-and-break` | 12 |  |
| 19 | `control-flow/07_while_condition` | while-loop | E0308 | `10b-while` | 13 |  |
| 20 | `control-flow/04_for_scope` | loop-variable-scope | E0425 | `10c-for-and-ranges` | 14 |  |
| 21 | `control-flow/02_match_exhaustive` | match-exhaustiveness | E0004 | `11-match-intro` | 15 |  |
| 22 | `control-flow/05_match_enum_data` | enum-data | E0532 | `19-enums` | 31 |  |
| 23 | `control-flow/08_or_pattern_binding` | or-pattern | E0408 | `19c-match-in-depth` | 33 |  |
| 24 | `collections/02_string_vs_str` | string-vs-str | E0308 | `12-string-vs-str` | 16 | ⬅ **back** |
| 25 | `collections/09_return_owned_string` | string-vs-str | E0308 | `12-string-vs-str` | 16 |  |
| 26 | `collections/03b_tuple_field` | tuple-field-access | E0609 | `13-tuples` | 17 |  |
| 27 | `collections/06b_array_length` | array-length-type | E0308 | `13b-arrays` | 18 |  |
| 28 | `collections/12_slice_borrow` | slice | E0308 | `13c-slices` | 19 |  |
| 29 | `collections/01_vec_push_type` | vec-element-type | E0308 | `14-vec` | 20 |  |
| 30 | `collections/06_index_out_of_bounds` | vec-index-bounds | — | `14-vec` | 20 |  |
| 31 | `collections/05_hashmap_insert` | hashmap-insert-ownership | E0382 | `14b-hashmap` | 21 |  |
| 32 | `collections/08_use_after_move_into_fn` | move-semantics | E0382 | `15b-moves` | 23 |  |
| 33 | `collections/07_fill_vec_needs_mut` | mutable-borrow-needs-mut | E0596 | `16b-mutable-references` | 26 |  |
| 34 | `collections/13_slice_holds_borrow` | slice-holds-borrow | E0502 | `17-slices-in-depth` | 27 |  |
| 35 | `collections/04_vec_moved` | iterate-by-reference | E0382 | `28-iterator-cursor` | 54 |  |
| 36 | `collections/03_iter_collect` | iterator-collect | E0308 | `28b-adapter-chains` | 55 |  |
| 37 | `ownership/10_explicit_drop_call` | explicit-drop | E0040 | `15-ownership` | 22 | ⬅ **back** |
| 38 | `ownership/01_move` | move-semantics | E0382 | `15b-moves` | 23 |  |
| 39 | `ownership/09_clone_string` | clone | E0382 | `15c-copy-and-clone` | 24 |  |
| 40 | `ownership/02b_deref_non_reference` | dereference-non-reference | E0614 | `16-shared-references` | 25 |  |
| 41 | `ownership/04_move_out_of_borrow` | move-out-of-borrow | E0507 | `16-shared-references` | 25 |  |
| 42 | `ownership/02_borrow` | borrow-checker-mut-vs-immut | E0502 | `16b-mutable-references` | 26 |  |
| 43 | `ownership/03_dangling` | no-dangling-refs | E0515 | `16b-mutable-references` | 26 |  |
| 44 | `ownership/05_double_mut_borrow` | borrow-checker-one-mut | E0499 | `16b-mutable-references` | 26 |  |
| 45 | `ownership/06_assign_while_borrowed` | assign-while-borrowed | E0506 | `16b-mutable-references` | 26 |  |
| 46 | `ownership/07_move_while_borrowed` | move-while-borrowed | E0505 | `16b-mutable-references` | 26 |  |
| 47 | `types/01_struct_build` | struct-construction | E0063 | `18-defining-structs` | 28 |  |
| 48 | `types/01b_struct_extra_field` | struct-construction | E0560 | `18-defining-structs` | 28 |  |
| 49 | `types/02_field_privacy` | struct-field-privacy | E0616 | `18-defining-structs` | 28 |  |
| 50 | `types/04_struct_method` | method-syntax | E0599 | `18b-methods-and-impl` | 29 |  |
| 51 | `types/08_derive_debug` | derive | E0277 | `18c-derive-debug` | 30 |  |
| 52 | `types/03_option_value` | option-extract-value | E0308 | `19b-option` | 32 |  |
| 53 | `types/05_refutable_let` | if-let | E0005 | `19d-concise-matching` | 34 |  |
| 54 | `types/05_unwrap_none` | option-unwrap-panic | — | `20b-panic-unwrap-expect` | 36 |  |
| 55 | `error-handling/01_result_is_not_t` | result-extract-value | E0308 | `20-result` | 35 | ⬅ **back** |
| 56 | `error-handling/03_unwrap_err_panics` | unwrap-panics-on-err | — | `20b-panic-unwrap-expect` | 36 |  |
| 57 | `error-handling/02_question_mark_propagates` | error-propagation | E0277 | `20c-question-mark` | 37 |  |
| 58 | `error-handling/04_custom_error_from` | custom-error-from | E0277 | `20c-question-mark` | 37 |  |
| 59 | `error-handling/05_question_mark_option_in_result` | question-mark-option-vs-result | E0277 | `20c-question-mark` | 37 |  |
| 60 | `modules/04_super_path` | super-paths | E0425 | `22-paths` | 41 |  |
| 61 | `modules/01_private_module` | module-privacy | E0603 | `22b-privacy-and-pub` | 42 |  |
| 62 | `modules/02_private_fn` | item-privacy | E0603 | `22b-privacy-and-pub` | 42 |  |
| 63 | `modules/03_use_scope` | use-and-paths | E0433 | `23-the-use-keyword` | 44 |  |
| 64 | `modules/05_unresolved_import` | use-and-paths | E0432 | `23-the-use-keyword` | 44 |  |
| 65 | `generics/01_bound` | generic-trait-bounds | E0369 | `24-generic-functions` | 45 |  |
| 66 | `generics/02_generic_struct_same_type` | generic-type-parameter | E0308 | `24b-generic-types` | 46 |  |
| 67 | `traits/01_display_bound` | trait-implementation | E0277 | `25-traits-declare-implement` | 47 |  |
| 68 | `traits/02_trait_in_scope` | trait-in-scope | E0599 | `25-traits-declare-implement` | 47 |  |
| 69 | `traits/04_missing_method` | trait-implementation | E0046 | `25-traits-declare-implement` | 47 |  |
| 70 | `lifetimes/03_borrow_outlives_value` | borrow-must-not-outlive-value | E0597 | `26-lifetime-annotations` | 49 |  |
| 71 | `lifetimes/04_temporary_dropped` | borrow-must-not-outlive-value | E0716 | `26-lifetime-annotations` | 49 |  |
| 72 | `lifetimes/01_longest` | lifetime-annotations | E0106 | `26-lifetime-annotations` | 49 |  |
| 73 | `lifetimes/02_struct_lifetime` | struct-lifetime | E0106 | `26c-lifetimes-in-structs` | 51 |  |
| 74 | `traits/03_trait_object_dyn` | trait-objects | E0308 | `32-trait-objects` | 65 |  |
| 75 | `closures/01_closure_type_lock` | closure-type-inference | E0308 | `27-closure-syntax` | 52 | ⬅ **back** |
| 76 | `closures/02_fn_once_move` | closure-move-capture | E0382 | `27b-closure-capture` | 53 |  |
| 77 | `iterators/02_into_iter_moves` | iterator-into-iter-ownership | E0382 | `28-iterator-cursor` | 54 |  |
| 78 | `iterators/01_collect_annotation` | iterator-collect-inference | E0283 | `28b-adapter-chains` | 55 |  |
| 79 | `iterators/03_sum_annotation` | iterator-consumer-sum | E0283 | `28b-adapter-chains` | 55 |  |
| 80 | `smart-pointers/01_recursive_box` | recursive-type-indirection | E0072 | `29-box` | 56 |  |
| 81 | `smart-pointers/02_shared_ownership_rc` | shared-ownership | E0382 | `29c-rc` | 58 |  |
| 82 | `smart-pointers/03_refcell_runtime_borrow` | interior-mutability | — | `29d-refcell` | 59 |  |
| 83 | `concurrency/01_spawn_move` | thread-move-closures | E0373 | `30-spawning-threads` | 60 |  |
| 84 | `concurrency/04_channel_send` | channel-message-passing | E0382 | `30b-channels` | 61 |  |
| 85 | `concurrency/02_rc_not_send` | send-across-threads | E0277 | `30c-shared-state` | 62 |  |
| 86 | `concurrency/03_arc_mutex` | shared-mutable-state | E0594 | `30c-shared-state` | 62 |  |
| 87 | `advanced/04_operator_overload_add` | operator-overloading | E0369 | `25-traits-declare-implement` | 47 | ⬅ **back** |
| 88 | `advanced/02_orphan_rule_newtype` | orphan-rule-newtype | E0117 | `25b-trait-bounds` | 48 |  |
| 89 | `advanced/05_supertrait_display` | supertrait | E0277 | `25b-trait-bounds` | 48 |  |
| 90 | `advanced/03_unsized_str` | dynamically-sized-types | E0277 | `32-trait-objects` | 65 |  |
| 91 | `advanced/01_unsafe_deref` | unsafe-raw-pointers | E0133 | `34-unsafe` | 70 |  |

**6 backward jumps** in the current order — the concrete reason the course doesn't walk Lesson 1 → 2 → 3 in step.


---


## 3 · Alignment analysis

**Status: re-sequenced (2026-07).** Exercises were re-ordered so each section marches in lesson order and the course now OPENS on Lesson 1 (`basics/08_use_before_init` → "Lesson 1 — Bindings & Immutability"). Ids stayed stable (order is the file-path order; progress is keyed by id), so the re-order was pure file renames + a `CONTENT_VERSION` bump.

**Remaining back-jumps: 6.** Each is a *category-boundary* outlier — an exercise whose lesson genuinely sits outside its category's range (e.g. a trait exercise filed under `advanced`, an enums exercise under `control-flow`). Closing these needs moving the exercise to another category, which changes its id (and would orphan existing progress), so they're deferred as a curriculum decision.

- **Pile-ups**: several lessons carry many exercises while others carry none:

  - **7×** `16b-mutable-references` — Lesson 16b — Mutable references & the borrowing rules

  - **4×** `25-traits-declare-implement` — Lesson 25 — Traits: declare and implement

  - **3×** `05b-integer-overflow` — Lesson 5b — Integer overflow: your first *runtime* panic

  - **3×** `07-functions` — Lesson 7 — Functions

  - **3×** `18-defining-structs` — Lesson 18 — Structs: bundle your data

  - **3×** `20c-question-mark` — Lesson 20c — The `?` operator: pass the problem up

  - **3×** `26-lifetime-annotations` — Lesson 26 — Lifetime annotations: the `longest` function

  - **3×** `28b-adapter-chains` — Lesson 28b — Adapter chains: `map`, `filter`, `collect`


**Proposed aligned order** (exercises sorted by the lesson they teach, then current order). A *starting point* for the re-sequence, not auto-applied — difficulty/dependencies may justify local tweaks:


| new # | Exercise | → Lesson | L# |
|-------|----------|----------|----|
| 1 | `basics/08_use_before_init` | `01-bindings-and-immutability` | 1 |
| 2 | `basics/01_immutable_assign` | `02-mutability` | 2 |
| 3 | `basics/09_shadow_type_change` | `03-shadowing` | 3 |
| 4 | `basics/10_const_needs_type` | `04-constants` | 4 |
| 5 | `basics/02_annotation_needed` | `05-scalar-types` | 5 |
| 6 | `basics/04_no_coercion` | `05-scalar-types` | 5 |
| 7 | `basics/05_integer_overflow` | `05b-integer-overflow` | 6 |
| 8 | `basics/05b_divide_by_zero` | `05b-integer-overflow` | 6 |
| 9 | `basics/05c_subtract_overflow` | `05b-integer-overflow` | 6 |
| 10 | `basics/11_semicolon_returns_unit` | `06-expressions-statements-semicolon` | 7 |
| 11 | `basics/03_fn_arg_type` | `07-functions` | 8 |
| 12 | `basics/07_fn_arg_count` | `07-functions` | 8 |
| 13 | `basics/07b_duplicate_fn_name` | `07-functions` | 8 |
| 14 | `basics/13_display_vs_debug` | `08-comments-and-printing` | 9 |
| 15 | `control-flow/06_bool_condition` | `09-if-else` | 10 |
| 16 | `control-flow/01_if_expr` | `09b-if-as-expression` | 11 |
| 17 | `control-flow/03_loop_break_value` | `10-loop-and-break` | 12 |
| 18 | `control-flow/07_while_condition` | `10b-while` | 13 |
| 19 | `control-flow/04_for_scope` | `10c-for-and-ranges` | 14 |
| 20 | `control-flow/02_match_exhaustive` | `11-match-intro` | 15 |
| 21 | `collections/02_string_vs_str` | `12-string-vs-str` | 16 |
| 22 | `collections/09_return_owned_string` | `12-string-vs-str` | 16 |
| 23 | `collections/03b_tuple_field` | `13-tuples` | 17 |
| 24 | `collections/06b_array_length` | `13b-arrays` | 18 |
| 25 | `collections/12_slice_borrow` | `13c-slices` | 19 |
| 26 | `collections/01_vec_push_type` | `14-vec` | 20 |
| 27 | `collections/06_index_out_of_bounds` | `14-vec` | 20 |
| 28 | `collections/05_hashmap_insert` | `14b-hashmap` | 21 |
| 29 | `ownership/10_explicit_drop_call` | `15-ownership` | 22 |
| 30 | `collections/08_use_after_move_into_fn` | `15b-moves` | 23 |
| 31 | `ownership/01_move` | `15b-moves` | 23 |
| 32 | `ownership/09_clone_string` | `15c-copy-and-clone` | 24 |
| 33 | `ownership/02b_deref_non_reference` | `16-shared-references` | 25 |
| 34 | `ownership/04_move_out_of_borrow` | `16-shared-references` | 25 |
| 35 | `basics/06_mutable_method_needs_mut` | `16b-mutable-references` | 26 |
| 36 | `collections/07_fill_vec_needs_mut` | `16b-mutable-references` | 26 |
| 37 | `ownership/02_borrow` | `16b-mutable-references` | 26 |
| 38 | `ownership/03_dangling` | `16b-mutable-references` | 26 |
| 39 | `ownership/05_double_mut_borrow` | `16b-mutable-references` | 26 |
| 40 | `ownership/06_assign_while_borrowed` | `16b-mutable-references` | 26 |
| 41 | `ownership/07_move_while_borrowed` | `16b-mutable-references` | 26 |
| 42 | `collections/13_slice_holds_borrow` | `17-slices-in-depth` | 27 |
| 43 | `types/01_struct_build` | `18-defining-structs` | 28 |
| 44 | `types/01b_struct_extra_field` | `18-defining-structs` | 28 |
| 45 | `types/02_field_privacy` | `18-defining-structs` | 28 |
| 46 | `types/04_struct_method` | `18b-methods-and-impl` | 29 |
| 47 | `types/08_derive_debug` | `18c-derive-debug` | 30 |
| 48 | `control-flow/05_match_enum_data` | `19-enums` | 31 |
| 49 | `types/03_option_value` | `19b-option` | 32 |
| 50 | `control-flow/08_or_pattern_binding` | `19c-match-in-depth` | 33 |
| 51 | `types/05_refutable_let` | `19d-concise-matching` | 34 |
| 52 | `error-handling/01_result_is_not_t` | `20-result` | 35 |
| 53 | `types/05_unwrap_none` | `20b-panic-unwrap-expect` | 36 |
| 54 | `error-handling/03_unwrap_err_panics` | `20b-panic-unwrap-expect` | 36 |
| 55 | `error-handling/02_question_mark_propagates` | `20c-question-mark` | 37 |
| 56 | `error-handling/04_custom_error_from` | `20c-question-mark` | 37 |
| 57 | `error-handling/05_question_mark_option_in_result` | `20c-question-mark` | 37 |
| 58 | `modules/04_super_path` | `22-paths` | 41 |
| 59 | `modules/01_private_module` | `22b-privacy-and-pub` | 42 |
| 60 | `modules/02_private_fn` | `22b-privacy-and-pub` | 42 |
| 61 | `modules/03_use_scope` | `23-the-use-keyword` | 44 |
| 62 | `modules/05_unresolved_import` | `23-the-use-keyword` | 44 |
| 63 | `generics/01_bound` | `24-generic-functions` | 45 |
| 64 | `generics/02_generic_struct_same_type` | `24b-generic-types` | 46 |
| 65 | `traits/01_display_bound` | `25-traits-declare-implement` | 47 |
| 66 | `traits/02_trait_in_scope` | `25-traits-declare-implement` | 47 |
| 67 | `traits/04_missing_method` | `25-traits-declare-implement` | 47 |
| 68 | `advanced/04_operator_overload_add` | `25-traits-declare-implement` | 47 |
| 69 | `advanced/02_orphan_rule_newtype` | `25b-trait-bounds` | 48 |
| 70 | `advanced/05_supertrait_display` | `25b-trait-bounds` | 48 |
| 71 | `lifetimes/03_borrow_outlives_value` | `26-lifetime-annotations` | 49 |
| 72 | `lifetimes/04_temporary_dropped` | `26-lifetime-annotations` | 49 |
| 73 | `lifetimes/01_longest` | `26-lifetime-annotations` | 49 |
| 74 | `lifetimes/02_struct_lifetime` | `26c-lifetimes-in-structs` | 51 |
| 75 | `closures/01_closure_type_lock` | `27-closure-syntax` | 52 |
| 76 | `closures/02_fn_once_move` | `27b-closure-capture` | 53 |
| 77 | `collections/04_vec_moved` | `28-iterator-cursor` | 54 |
| 78 | `iterators/02_into_iter_moves` | `28-iterator-cursor` | 54 |
| 79 | `collections/03_iter_collect` | `28b-adapter-chains` | 55 |
| 80 | `iterators/01_collect_annotation` | `28b-adapter-chains` | 55 |
| 81 | `iterators/03_sum_annotation` | `28b-adapter-chains` | 55 |
| 82 | `smart-pointers/01_recursive_box` | `29-box` | 56 |
| 83 | `smart-pointers/02_shared_ownership_rc` | `29c-rc` | 58 |
| 84 | `smart-pointers/03_refcell_runtime_borrow` | `29d-refcell` | 59 |
| 85 | `concurrency/01_spawn_move` | `30-spawning-threads` | 60 |
| 86 | `concurrency/04_channel_send` | `30b-channels` | 61 |
| 87 | `concurrency/02_rc_not_send` | `30c-shared-state` | 62 |
| 88 | `concurrency/03_arc_mutex` | `30c-shared-state` | 62 |
| 89 | `traits/03_trait_object_dyn` | `32-trait-objects` | 65 |
| 90 | `advanced/03_unsized_str` | `32-trait-objects` | 65 |
| 91 | `advanced/01_unsafe_deref` | `34-unsafe` | 70 |

---


## 4 · Lessons with no exercise yet (gap list)

20 of 78 lessons have no paired exercise — candidates for new authoring. In learning order:

- `00-hello-world` — **Lesson 0 — Hello, World (your first program)** — Teaches the skeleton of every Rust program: an entry point that runs and prints text to the screen.
- `21-packages-and-crates` — **Lesson 21 — Packages & crates: what you've been building all along** — Names the units of a Rust project: a crate is the smallest unit the compiler builds (binary with src/main.rs or library with src/lib.rs) and a package is a Cargo.toml plus one or more crates.
- `21b-modules` — **Lesson 21b — Modules & the module tree** — A module (`mod`) is a named group inside a crate that provides organization and privacy, and nested modules form a tree rooted at `crate` addressed by `::` paths.
- `21c-modules-in-files` — **Lesson 21c — Splitting modules into files** — It teaches how a `mod name;` declaration (semicolon, no body) tells the compiler to load a module's body from a separate file so the directory layout mirrors the module tree.
- `22c-pub-structs-enums` — **Lesson 22c — `pub` on structs & enums: an asymmetry** — It teaches the asymmetry that a `pub struct` keeps its fields private (requiring a public constructor) while a `pub enum` makes all its variants public.
- `26b-lifetime-elision` — **Lesson 26b — Lifetime elision: the annotations you never write** — It explains the compiler's lifetime elision rules — chiefly that a function with one input reference donates that lifetime to all outputs — so you know which functions demand a manual annotation and which don't.
- `29b-deref-drop` — **Lesson 29b — `Deref` & `Drop`: the traits underneath** — It explains the two traits behind smart pointers: Deref (why * follows a box like a reference) and Drop (an automatic cleanup hook that runs at scope-end in reverse creation order).
- `31-async-syntax` — **Lesson 31 — Async syntax: `async fn`, `.await` & async blocks** — The shape of async code: async fn returns a future rather than its value, async blocks are future-valued expressions, and .await runs a future to completion inside async contexts.
- `31b-futures-are-lazy` — **Lesson 31b — Futures are lazy: why async needs a runtime** — The core async rule that a future does nothing until driven, which is why async code needs a runtime and why .await is restricted and main cannot be async on its own.
- `32b-encapsulation` — **Lesson 32b — Encapsulation: private fields, public methods** — How keeping struct fields private and exposing only public methods enforces invariants in one place and lets you change internals without breaking callers.
- `32c-states-as-types` — **Lesson 32c — States as types: broken states won't compile** — How giving each stage its own type (e.g. DraftPost vs PublishedPost) makes wrong-state operations unrepresentable, with transitions that consume self to retire the old state.
- `33-refutability` — **Lesson 33 — Refutability: when a pattern can fail** — The refutability rule that governs where patterns may sit: irrefutable patterns (always match) are needed for let/for/params, while refutable ones (can fail) require an else path via if let/while let/match.
- `33b-guards-bindings-nested` — **Lesson 33b — Guards, `@` bindings & nested patterns** — Three pattern power tools for match: match guards add a value condition to an arm, @ bindings capture a value while range-testing it, and nested destructuring reaches into layered data.
- `34b-operator-overloading` — **Lesson 34b — Operator overloading & associated types** — It teaches operator overloading by implementing std::ops traits like Add on your own type, and explains associated types (type Output) so you can read forms like Future<Output = u32>.
- `34c-macro-rules` — **Lesson 34c — Declarative macros: `macro_rules!`** — It teaches declarative macros with macro_rules! — pattern-to-template rules that generate code before compilation — by building a stripped-down my_vec! macro.
- `35-single-threaded-server` — **Lesson 35 — Capstone I: a single-threaded web server** — It builds a real single-threaded HTTP server with TcpListener/TcpStream so the learner feels why handling connections inline (one at a time) stalls behind a slow request.
- `35b-thread-pool` — **Lesson 35b — Capstone II: the thread pool & graceful shutdown** — It fixes the single-threaded server by building a fixed-size thread pool with workers pulling boxed closure jobs from a shared channel, plus graceful shutdown via Drop and join.
- `36-automated-tests` — **Lesson 36 — Automated Tests** — It teaches writing automated tests that pass when they don't panic, using #[test] functions, assertion macros, and #[should_panic], run by cargo test.
- `37-doc-comments` — **Lesson 37 — Documenting Rust: doc comments & doc-tests** — It teaches Markdown doc comments (/// and //!) that cargo doc renders into an HTML manual, and doc-tests — code examples that cargo test compiles and runs so docs can't drift from code.
- `37b-shipping-with-cargo` — **Lesson 37b — Shipping with Cargo: profiles, publishing & workspaces** — It surveys Cargo's shipping toolkit: release vs dev build profiles, publishing crates to crates.io with required metadata, workspaces of related crates, and cargo install.

---


## 5 · Per-lesson detail (subject + what it teaches)

The authoring reference: every lesson's subject and the concrete things it covers.


**L0 · Lesson 0 — Hello, World (your first program)** — `lessons/00-hello-world.md`  
Teaches the skeleton of every Rust program: an entry point that runs and prints text to the screen.  
Teaches: fn main entry point; println! macro and the ! marker; statements and the semicolon; curly-brace body blocks; E0423 (macro used as a function)

**L1 · Lesson 1 — Bindings & Immutability** — `lessons/01-bindings-and-immutability.md`  
Teaches that naming a value with let binds it immutably by default so it can't be reassigned later.  
Teaches: let bindings; immutability by default; type inference; explicit type annotation (: i32); E0384 (cannot assign twice to immutable variable)

**L2 · Lesson 2 — Mutability (`mut`)** — `lessons/02-mutability.md`  
Teaches how mut opts a binding into being changeable while keeping its type fixed.  
Teaches: let mut; reassignment of mutable values; type stays fixed after first assignment; E0308 (mismatched types); &str vs integer

**L3 · Lesson 3 — Shadowing** — `lessons/03-shadowing.md`  
Teaches shadowing: a second let with the same name makes a new binding that can change the value's type, unlike mut.  
Teaches: shadowing via repeated let; changing a value's type across bindings; scope and block-local shadows; method calls (.trim(), .len()); shadowing vs mutation

**L4 · Lesson 4 — Constants (`const`)** — `lessons/04-constants.md`  
Teaches const for values fixed forever and known at compile time, requiring an explicit type.  
Teaches: const declarations; required type annotation; SCREAMING_SNAKE_CASE convention; compile-time / file-scope constants; 'missing type for const item' error

**L5 · Lesson 5 — The scalar types: integers, floats, `bool` & `char`** — `lessons/05-scalar-types.md`  
Teaches Rust's four scalar types and that Rust never converts between number types silently.  
Teaches: integers (i32, u32, u8, i64) and signed/unsigned widths; floats (f64); bool; char with single quotes; no silent numeric conversion (E0308)

**L6 · Lesson 5b — Integer overflow: your first *runtime* panic** — `lessons/05b-integer-overflow.md`  
Teaches that arithmetic overflowing an integer's width causes a runtime panic in debug builds rather than silent wrapping.  
Teaches: integer overflow; runtime panic vs compile-time error; u8 range (0-255); debug vs release build overflow behavior; 'attempt to add with overflow' panic

**L7 · Lesson 6 — Expressions, statements & the semicolon** — `lessons/06-expressions-statements-semicolon.md`  
Teaches the difference between expressions (produce values) and statements, and how a semicolon discards a value to produce unit ().  
Teaches: expressions vs statements; semicolon discards a value; unit type (); blocks as expressions (last line without semicolon); E0308 from a stray semicolon in a returning function

**L8 · Lesson 7 — Functions** — `lessons/07-functions.md`  
Teaches declaring functions with typed parameters and a return type, returning via the last expression.  
Teaches: fn declarations and snake_case names; typed parameters (required types); return type with ->; return-by-last-expression; E0308 (wrong argument type)

**L9 · Lesson 8 — Comments & printing** — `lessons/08-comments-and-printing.md`  
Teaches comment syntax and the full range of printing with println!, format!, and Display vs Debug formatting.  
Teaches: comments (//, /* */, ///); println! with {name} and positional {} placeholders; format! returning a String; {} Display vs {:?} Debug; E0277 (type doesn't implement Display)

**L10 · Lesson 9 — `if` / `else if` / `else`: making the program choose** — `lessons/09-if-else.md`  
Teaches branching: how to run one block of code when a condition is true and another otherwise, using if/else if/else with real bool conditions (no truthiness).  
Teaches: if / else if / else; bool conditions and no-truthiness rule; comparison operators (>, ==, !=); first-true-wins ordering; E0308 mismatched types (non-bool condition)

**L11 · Lesson 9b — `if` is an expression: branching that produces a value** — `lessons/09b-if-as-expression.md`  
Teaches that an if/else is an expression that produces a value, so it can sit on the right of a let, with the requirement that every arm produce the same type.  
Teaches: if as an expression; let number = if condition { 5 } else { 6 }; arms must agree on type; block value / semicolon rule; E0308 incompatible if/else arm types

**L12 · Lesson 10 — `loop`: repeat until you `break`** — `lessons/10-loop-and-break.md`  
Teaches the loop keyword that repeats forever until break, and its unique power to carry a value out via break <value> because it has exactly one exit.  
Teaches: loop and break; break with a value; compound assignment (+=); loop labels (mentioned); E0571 break with value from a while loop

**L13 · Lesson 10b — `while`: repeat while a condition holds** — `lessons/10b-while.md`  
Teaches the while loop that repeats while a bool condition holds, checking the condition each pass and stopping when it becomes false.  
Teaches: while loop; bool condition requirement; compound assignment (-=); why while cannot break with a value; E0308 mismatched types (non-bool while condition)

**L14 · Lesson 10c — `for` & ranges: walk each item** — `lessons/10c-for-and-ranges.md`  
Teaches the for loop that walks each item of a collection or range without managing an index, so it cannot run off the end.  
Teaches: for ... in loop; ranges: exclusive 1..4 vs inclusive 1..=4; print! vs println!; loop-variable scope; E0425 cannot find value in this scope

**L15 · Lesson 11 — `match` (intro)** — `lessons/11-match-intro.md`  
Teaches match: hand it one value, list cases as patterns, and it runs the first arm that fits, with compiler-enforced exhaustiveness.  
Teaches: match with pattern => result arms; _ catch-all wildcard; exhaustiveness checking; match as an expression; E0004 non-exhaustive patterns

**L16 · Lesson 12 — `String` vs `&str`** — `lessons/12-string-vs-str.md`  
Teaches the difference between a borrowed fixed &str view and an owned growable String, and how to build and grow a String.  
Teaches: &str (borrowed view) vs String (owned, growable); String::from / to_string; push_str, format!, as_str; no integer indexing of strings (UTF-8); E0277 str cannot be indexed by integer

**L17 · Lesson 13 — Tuples: grouping mixed values** — `lessons/13-tuples.md`  
Teaches tuples: bundling a fixed number of values of different types into one value, accessed by destructuring or by position.  
Teaches: tuples of mixed types; arity as part of the type; destructuring vs .0/.1/.2 access; unit type (); E0308 mismatched types (wrong destructure arity)

**L18 · Lesson 13b — Arrays: a fixed row of one type** — `lessons/13b-arrays.md`  
Teaches arrays: a fixed-length row of same-type values whose length is part of the type [T; N], with bounds checks caught at compile time or run time.  
Teaches: arrays [T; N] and [0; 3] shorthand; indexing and .len(); compile-time vs runtime bounds checks (panic); off-by-one (<= vs <) walk; for as the safe fix for indexing

**L19 · Lesson 13c — Slices: a borrowed window** — `lessons/13c-slices.md`  
Teaches slices: a borrowed window &[T] into part of a sequence whose type erases the length, so one function accepts any size.  
Teaches: slices &[i32] via &scores[1..3]; length erased from the type; &str is a slice of a String; borrowing marker &; runtime panic on out-of-range slice / UTF-8 char-boundary

**L20 · Lesson 14 — `Vec`: the growable list** — `lessons/14-vec.md`  
Teaches Vec<T>, the growable list whose length lives at run time, and the deliberate choice between indexing (which panics on a bad index) and .get() (which returns an Option).  
Teaches: Vec<T>; vec! / push / pop; [] vs .get() (panic vs Option); Vec::new(); runtime index-out-of-bounds panic

**L21 · Lesson 14b — `HashMap`: the lookup table** — `lessons/14b-hashmap.md`  
Teaches HashMap<K,V> as a key-to-value lookup table, including the required import, the entry/or_insert counting idiom, and its unordered nature.  
Teaches: HashMap<K, V>; use std::collections::HashMap; insert / get (Option); entry().or_insert() counting; E0433 (undeclared type import error)

**L22 · Lesson 15 — Ownership, scope & drop** — `lessons/15-ownership.md`  
Teaches Rust's ownership model: every value has one owner, and when the owner goes out of scope the value is dropped and freed automatically at build-time-proven safety.  
Teaches: the three rules of ownership; owner / scope / drop; automatic freeing at end of block; stack vs heap (one-sentence model); E0425 (value not in scope)

**L23 · Lesson 15b — Moves: assignment hands ownership over** — `lessons/15b-moves.md`  
Teaches that assigning or passing a String moves ownership (invalidating the old name) so a value is never freed twice, and using the moved value is Rust's most famous compile error.  
Teaches: move semantics; String assignment / function-argument moves; use-after-move; E0382 (borrow of moved value); preview of borrowing as the fix

**L24 · Lesson 15c — Copy & Clone: when assignment duplicates** — `lessons/15c-copy-and-clone.md`  
Teaches the Copy trait (why small stack values duplicate on assignment instead of moving) versus non-Copy types that move, and .clone() as the explicit, visible deep copy.  
Teaches: Copy trait; Clone / .clone() deep copy; Copy vs move distinction; Copy-or-custom-drop-never-both rule; reading the Copy line in E0382

**L25 · Lesson 16 — Shared references: borrow to *read*** — `lessons/16-shared-references.md`  
Teaches shared references (&T) that let code borrow a value to read it without taking ownership, so the original owner keeps the value and can have many simultaneous read-only borrows.  
Teaches: shared reference &T; borrowing to read; &s / passing &String; many simultaneous shared borrows; E0507 (cannot move out of a shared reference)

**L26 · Lesson 16b — Mutable references & the borrowing rules** — `lessons/16b-mutable-references.md`  
Teaches mutable references (&mut T) for borrowing to change a value, plus the two borrowing rules (shared xor mutable, references must stay valid) enforced by the borrow checker.  
Teaches: mutable reference &mut T; shared xor mutable rule; E0499 (two mutable borrows); E0502 (mutable while shared); E0597 (reference outlives value)

**L27 · Lesson 17 — Slices in Depth** — `lessons/17-slices-in-depth.md`  
Teaches that a slice is a borrow pointing into a collection, so while the slice is alive the collection is borrowed immutably — which turns stale-index bugs into compile errors.  
Teaches: slices as borrows (&[T], &str); fn first_word(s: &str) -> &str; slice pins the collection immutably; stale-index bug prevention; E0502 (mutate while slice/reference alive)

**L28 · Lesson 18 — Structs: bundle your data** — `lessons/18-defining-structs.md`  
Teaches defining and instantiating structs to group related fields into one named owned type, including field-init shorthand, struct update, tuple structs, and unit structs.  
Teaches: struct definition & instantiation; field access & field-init shorthand; struct update (..), tuple & unit structs; owned fields (String not &str); E0106 (missing lifetime specifier)

**L29 · Lesson 18b — Methods & `impl` blocks** — `lessons/18b-methods-and-impl.md`  
Teaches attaching behaviour to structs via impl blocks and methods, where the receiver (&self / &mut self / self) mirrors the Phase-4 borrow rules, plus associated functions called with ::.  
Teaches: impl blocks & methods; receivers &self / &mut self / self; associated functions & Self / ::; self consumes (E0382 callback); E0599 (no method found)

**L30 · Lesson 18c — Printing your own types: `#[derive(Debug)]`** — `lessons/18c-derive-debug.md`  
Adding `#[derive(Debug)]` above a struct makes it printable with `{:?}` (compact) and `{:#?}` (pretty), and the compiler literally tells you to add that line when it's missing.  
Teaches: #[derive(Debug)]; {:?} vs {:#?} formatting; E0277 (type doesn't implement Debug); dbg! macro; compiler-suggested fixes

**L31 · Lesson 19 — Enums: one-of types** — `lessons/19-enums.md`  
An enum models a value that is one of several shapes, with variants that can be empty or carry data, and you attach methods with impl that match on self.  
Teaches: enum definition; variant kinds (unit, struct-like, tuple-like); variant as constructor; impl + match on self; E0532 (wrong variant shape)

**L32 · Lesson 19b — `Option<T>`: Rust has no null** — `lessons/19b-option.md`  
Rust has no null; instead the possibility of a missing value lives in the type `Option<T>` (Some/None), so the compiler forces you to handle absence.  
Teaches: Option<T>; Some / None variants; no-null design; E0004 (non-exhaustive match); E0277 (Option<T> is not a T)

**L33 · Lesson 19c — `match` patterns in depth** — `lessons/19c-match-in-depth.md`  
The full match pattern vocabulary — binding inner data, multiple patterns, ranges, guards, and catch-alls — while the compiler still enforces exhaustiveness.  
Teaches: binding patterns; | multiple patterns; ..= ranges; match guards (if); catch-all vs _; unreachable pattern warning

**L34 · Lesson 19d — Concise matching: `if let`, `while let`, `let…else`** — `lessons/19d-concise-matching.md`  
Three short forms — `if let`, `while let`, and `let…else` — handle a single case by deliberately trading away exhaustiveness checking to keep the happy path flat.  
Teaches: if let; while let; let…else (diverging else); irrefutable vs refutable patterns; E0005 (refutable pattern in local binding)

**L35 · Lesson 20 — `Result<T, E>`: errors are values** — `lessons/20-result.md`  
In Rust a recoverable error is an ordinary value carried in the return type as `Result<T, E>` (Ok/Err), which you read with match or unwrap_or rather than hidden exceptions.  
Teaches: Result<T, E>; Ok / Err variants; match on a Result; unwrap_or(default); str::parse / turbofish; E0308 (mismatched types)

**L36 · Lesson 20b — `panic!`, `unwrap` and `expect`: the blunt path** — `lessons/20b-panic-unwrap-expect.md`  
The blunt error path stops the program on the spot: `panic!` halts the thread, and `unwrap`/`expect` give the Ok/Some value or panic — a runtime crash, not a compile error.  
Teaches: panic!; .unwrap(); .expect("context"); runtime panic vs compile error; reading panic messages (ParseIntError/InvalidDigit)

**L37 · Lesson 20c — The `?` operator: pass the problem up** — `lessons/20c-question-mark.md`  
The `?` operator is a whole error-handling match in one character: on Err it early-returns the error from the enclosing function, and on Ok it unwraps and continues.  
Teaches: ? operator; error propagation / early return; function must return Result or Option; From trait error conversion; E0277 (? in non-Result function)

**L38 · Lesson 21 — Packages & crates: what you've been building all along** — `lessons/21-packages-and-crates.md`  
Names the units of a Rust project: a crate is the smallest unit the compiler builds (binary with src/main.rs or library with src/lib.rs) and a package is a Cargo.toml plus one or more crates.  
Teaches: crate (build unit); binary vs library crate; crate root (main.rs / lib.rs); package + Cargo.toml; fn main requirement; E0601 (main function not found)

**L39 · Lesson 21b — Modules & the module tree** — `lessons/21b-modules.md`  
A module (`mod`) is a named group inside a crate that provides organization and privacy, and nested modules form a tree rooted at `crate` addressed by `::` paths.  
Teaches: mod (module definition); module tree (crate root, parent/child/sibling); :: paths / namespaces; pub (privacy preview); E0425 (function not found in scope)

**L40 · Lesson 21c — Splitting modules into files** — `lessons/21c-modules-in-files.md`  
It teaches how a `mod name;` declaration (semicolon, no body) tells the compiler to load a module's body from a separate file so the directory layout mirrors the module tree.  
Teaches: mod name; file-loading declaration; src/name.rs and submodule src/name/vegetables.rs layout; the older name/mod.rs style; "mod is not an include" (declared once, referred to by path); E0583 file not found for module

**L41 · Lesson 22 — Paths: naming items across modules** — `lessons/22-paths.md`  
It teaches how to name an item in another module using `::`-separated paths, distinguishing absolute (crate::) from relative (self::/super::/sibling) forms.  
Teaches: absolute paths with crate::; relative paths and super:: / self::; child modules can call ancestors' private items; prefer-absolute-paths guidance; E0425 cannot find function in this scope

**L42 · Lesson 22b — Privacy & `pub`: private by default** — `lessons/22b-privacy-and-pub.md`  
It teaches that every item is private to code outside its module by default and that `pub` opens one specific item, with the trap that `pub mod` does not make the module's contents public.  
Teaches: private-by-default visibility; pub opens a single item; pub mod does not publish contents (two-strike rule); pub(crate) scope; modules are the privacy boundary; E0603 function is private

**L43 · Lesson 22c — `pub` on structs & enums: an asymmetry** — `lessons/22c-pub-structs-enums.md`  
It teaches the asymmetry that a `pub struct` keeps its fields private (requiring a public constructor) while a `pub enum` makes all its variants public.  
Teaches: pub struct with per-field privacy; public constructor / associated function requirement; pub enum opens all variants; privacy is module-based not type-based; E0616 private field (and E0451 for struct literals)

**L44 · Lesson 23 — The `use` Keyword** — `lessons/23-the-use-keyword.md`  
It teaches how the `use` keyword brings a path into scope once so code can call items by their short name, plus handling name clashes with `as` and the scope-locality trap.  
Teaches: use to shorten paths; parent-module-for-functions vs full-path-for-types idiom; as for renaming to avoid clashes; pub use re-exporting and glob/nested imports; use is scope-local; E0433 unresolved module in this scope

**L45 · Lesson 24 — Generic functions: the `<T>` placeholder** — `lessons/24-generic-functions.md`  
It teaches how a generic type parameter `<T>` lets one function work for many types by leaving a type hole filled at each call, ending copy-paste-and-rename duplication.  
Teaches: type parameter <T> declaration and use; a generic body must be valid for every T; Vec<T> and Option<T> as generics already used; E0369 binary operation > cannot be applied to &T; trait bound preview (PartialOrd)

**L46 · Lesson 24b — Generic structs, enums & methods** — `lessons/24b-generic-types.md`  
It teaches how to put the `<T>` placeholder into struct definitions, enum definitions, and impl blocks so one type definition serves every element type.  
Teaches: generic structs struct Point<T>; generic enums (how Option<T> is built); impl<T> Point<T> with <T> declared after impl; monomorphization (zero runtime cost); E0425 cannot find type T in this scope

**L47 · Lesson 25 — Traits: declare and implement** — `lessons/25-traits-declare-implement.md`  
It teaches how to declare a trait as a set of method signatures and implement it for a type with `impl Trait for Type`, distinguishing required methods from default methods.  
Teaches: trait keyword and method signatures; impl Trait for Type; required (no body) vs default (with body) methods; traits must be in scope to call; the orphan rule; E0046 not all trait items implemented

**L48 · Lesson 25b — Trait bounds: demanding behaviour from a generic `T`** — `lessons/25b-trait-bounds.md`  
It teaches how a trait bound constrains a generic `T` to types implementing a trait so the body may use the trait's behaviour, closing Lesson 24's E0369 wall.  
Teaches: impl Trait parameter syntax; generic bound <T: Summary>; where clauses; PartialOrd bound resolving largest<T>; E0277 trait bound not satisfied

**L49 · Lesson 26 — Lifetime annotations: the `longest` function** — `lessons/26-lifetime-annotations.md`  
It teaches lifetime annotations (`'a`) as generics for how long references stay valid, needed when a function returns one of several input references so the compiler knows which it borrows from.  
Teaches: lifetime annotation syntax 'a; fn longest<'a>(x: &'a str, y: &'a str) -> &'a str; annotations describe relationships, they don't change lifespans; E0106 missing lifetime specifier; E0597 does not live long enough

**L50 · Lesson 26b — Lifetime elision: the annotations you never write** — `lessons/26b-lifetime-elision.md`  
It explains the compiler's lifetime elision rules — chiefly that a function with one input reference donates that lifetime to all outputs — so you know which functions demand a manual annotation and which don't.  
Teaches: lifetime elision rules; single-input-reference rule; returning references without annotations; E0515 (returning a reference to a local); &str slices

**L51 · Lesson 26c — Lifetimes in structs: closing the Lesson 18 promise** — `lessons/26c-lifetimes-in-structs.md`  
It teaches how to declare a lifetime on a struct so a field can hold a borrowed reference (&'a str) without owning it, guaranteeing the struct never outlives the data it borrows.  
Teaches: struct lifetime parameter <'a>; borrowed &'a str fields; borrow vs own for struct fields; E0106 (missing lifetime specifier); struct that borrows a slice

**L52 · Lesson 27 — Closures: unnamed inline functions** — `lessons/27-closure-syntax.md`  
It introduces closures as anonymous inline functions written with |param| bars, saved in variables or passed to methods, whose parameter/return types are inferred and locked on first use.  
Teaches: closure syntax with |bars|; type inference locked on first use; passing closures to methods like sort_by_key; E0308 (mismatched types); closures vs named fn

**L53 · Lesson 27b — Closure capture & `move`** — `lessons/27b-closure-capture.md`  
It teaches how closures capture surrounding variables in one of three ways (immutable borrow, mutable borrow, or take ownership), with the body deciding the mode and the move keyword forcing ownership.  
Teaches: three capture modes (borrow/mut-borrow/own); move keyword; Fn/FnMut/FnOnce trait family; E0382 (use of moved value); E0507 (cannot move out of captured variable)

**L54 · Lesson 28 — Iterators: the `next()` cursor** — `lessons/28-iterator-cursor.md`  
It reveals that a for loop is driven by an iterator — a cursor implementing the Iterator trait whose next() returns Some/None — and that iter/iter_mut/into_iter choose read, mutate, or take-ownership access.  
Teaches: Iterator trait and next(); iter vs iter_mut vs into_iter; iterator laziness; Option Some/None from next; E0382 (move via into_iter)

**L55 · Lesson 28b — Adapter chains: `map`, `filter`, `collect`** — `lessons/28b-adapter-chains.md`  
It teaches building readable iterator pipelines from lazy adapters (map, filter) plus a consumer (collect, sum) that actually drives the work, and why a chain with no consumer does nothing.  
Teaches: map and filter adapters; collect consumer and its type annotation; sum consumer; iterator laziness / unused_must_use warning; adapters vs consumers

**L56 · Lesson 29 — `Box<T>`: values on the heap** — `lessons/29-box.md`  
It introduces Box<T> as a smart pointer that stores a value on the heap behind a fixed-size pointer, chiefly to give recursive self-containing types a known size via indirection.  
Teaches: Box<T> heap allocation; recursive types (cons list); indirection to break infinite size; E0072 (recursive type has infinite size); Box::new

**L57 · Lesson 29b — `Deref` & `Drop`: the traits underneath** — `lessons/29b-deref-drop.md`  
It explains the two traits behind smart pointers: Deref (why * follows a box like a reference) and Drop (an automatic cleanup hook that runs at scope-end in reverse creation order).  
Teaches: Deref trait and the * operator; Drop trait and drop method; automatic cleanup at scope end; reverse creation drop order; smart pointer internals

**L58 · Lesson 29c — `Rc<T>`: one value, many owners** — `lessons/29c-rc.md`  
It teaches Rc<T> as a reference-counted smart pointer enabling multiple shared read-only owners of one heap value, cleaned up only when the count reaches zero.  
Teaches: Rc<T> reference counting; Rc::clone (cheap count bump, not data copy); Rc::strong_count; shared read-only ownership; Drop-driven count decrement

**L59 · Lesson 29d — `RefCell<T>`: interior mutability** — `lessons/29d-refcell.md`  
It teaches RefCell<T> as interior mutability — mutating a value through a shared, immutable-looking handle by moving the borrow check to runtime, panicking on violation, and combining with Rc as Rc<RefCell<T>>.  
Teaches: RefCell<T> interior mutability; borrow() and borrow_mut(); runtime borrow checking (panic vs compile error); Rc<RefCell<T>> combination; 'already borrowed' runtime panic

**L60 · Lesson 30 — Spawning threads: `spawn`, `join` & `move`** — `lessons/30-spawning-threads.md`  
How to start a second line of work with a thread and safely hand it data, using thread::spawn to run a closure, join to wait for it, and move to give the thread ownership so borrows can't dangle.  
Teaches: thread::spawn; JoinHandle / join; move closures; data races and fearless concurrency; E0373

**L61 · Lesson 30b — Channels: passing data between threads** — `lessons/30b-channels.md`  
How threads cooperate without sharing by sending values through a one-way channel, where send moves ownership so the sender can no longer touch the value.  
Teaches: mpsc::channel; tx.send / rx.recv; receiving by iterating rx; multiple producers via tx.clone; ownership moves on send; E0382

**L62 · Lesson 30c — Shared state: `Arc` & `Mutex`** — `lessons/30c-shared-state.md`  
How multiple threads safely share and mutate one value using Arc for many owners and Mutex for exclusive access, with the compiler's errors steering you to the Arc<Mutex<T>> design.  
Teaches: Mutex<T> and .lock(); Arc<T>; Arc<Mutex<T>>; Send and Sync marker traits; E0382, E0277, E0594

**L63 · Lesson 31 — Async syntax: `async fn`, `.await` & async blocks** — `lessons/31-async-syntax.md`  
The shape of async code: async fn returns a future rather than its value, async blocks are future-valued expressions, and .await runs a future to completion inside async contexts.  
Teaches: async fn returning impl Future; async { } blocks; .await; async vs threads (cooperative concurrency vs parallelism); E0308

**L64 · Lesson 31b — Futures are lazy: why async needs a runtime** — `lessons/31b-futures-are-lazy.md`  
The core async rule that a future does nothing until driven, which is why async code needs a runtime and why .await is restricted and main cannot be async on its own.  
Teaches: futures are lazy (like iterators); runtimes / executors; .await only in async contexts; async fn main is not allowed; E0728, E0752

**L65 · Lesson 32 — Trait objects: one collection, many types** — `lessons/32-trait-objects.md`  
How dyn Trait behind a pointer lets one collection or function hold many different concrete types and call their shared method via runtime dynamic dispatch.  
Teaches: Box<dyn Trait> and &dyn Trait; dynamic vs static dispatch; fat pointers and vtables; dyn compatibility; E0308, E0038

**L66 · Lesson 32b — Encapsulation: private fields, public methods** — `lessons/32b-encapsulation.md`  
How keeping struct fields private and exposing only public methods enforces invariants in one place and lets you change internals without breaking callers.  
Teaches: private fields vs pub methods; module privacy boundaries; invariants via private helpers; changing internals without breaking callers; E0616

**L67 · Lesson 32c — States as types: broken states won't compile** — `lessons/32c-states-as-types.md`  
How giving each stage its own type (e.g. DraftPost vs PublishedPost) makes wrong-state operations unrepresentable, with transitions that consume self to retire the old state.  
Teaches: one type per state (typestate); methods only where valid; transitions consuming self by value; invalid states unrepresentable; E0599

**L68 · Lesson 33 — Refutability: when a pattern can fail** — `lessons/33-refutability.md`  
The refutability rule that governs where patterns may sit: irrefutable patterns (always match) are needed for let/for/params, while refutable ones (can fail) require an else path via if let/while let/match.  
Teaches: irrefutable vs refutable patterns; let/for/params need irrefutable; if let / let...else provide the else path; irrefutable if let is only a warning; E0005

**L69 · Lesson 33b — Guards, `@` bindings & nested patterns** — `lessons/33b-guards-bindings-nested.md`  
Three pattern power tools for match: match guards add a value condition to an arm, @ bindings capture a value while range-testing it, and nested destructuring reaches into layered data.  
Teaches: match guards (if condition on an arm); @ bindings (name @ pattern); nested destructuring; guards ignored by exhaustiveness; E0004

**L70 · Lesson 34 — `unsafe`: a small, audited escape hatch** — `lessons/34-unsafe.md`  
It teaches that `unsafe` is a small, auditable block that unlocks five compiler-unverifiable abilities (chiefly dereferencing raw pointers) without disabling the borrow checker.  
Teaches: unsafe blocks and the five superpowers; raw pointers *const T / *mut T and &raw const / &raw mut; dereferencing a raw pointer requires unsafe (E0133); unsafe does not turn off borrow checking (E0502)

**L71 · Lesson 34b — Operator overloading & associated types** — `lessons/34b-operator-overloading.md`  
It teaches operator overloading by implementing std::ops traits like Add on your own type, and explains associated types (type Output) so you can read forms like Future<Output = u32>.  
Teaches: std::ops operator traits (Add, Sub, Mul); a + b is sugar for Add::add(a, b); associated types via type Output and type Item; reading Trait<Output = T> angle-bracket form; E0369 cannot add before implementing Add

**L72 · Lesson 34c — Declarative macros: `macro_rules!`** — `lessons/34c-macro-rules.md`  
It teaches declarative macros with macro_rules! — pattern-to-template rules that generate code before compilation — by building a stripped-down my_vec! macro.  
Teaches: macro_rules! pattern => template rules; captures like $x:expr (and ident, ty, stmt); repetition $( ... ),* in pattern and template; the trailing-comma fix $(,)?; macros expand before compilation, marked by !

**L73 · Lesson 35 — Capstone I: a single-threaded web server** — `lessons/35-single-threaded-server.md`  
It builds a real single-threaded HTTP server with TcpListener/TcpStream so the learner feels why handling connections inline (one at a time) stalls behind a slow request.  
Teaches: TcpListener::bind and listener.incoming(); TcpStream reading and write_all; anatomy of an HTTP response (status line, headers, blank line, body); the inline single-threaded blocking flaw; E0596 missing mut on the stream parameter

**L74 · Lesson 35b — Capstone II: the thread pool & graceful shutdown** — `lessons/35b-thread-pool.md`  
It fixes the single-threaded server by building a fixed-size thread pool with workers pulling boxed closure jobs from a shared channel, plus graceful shutdown via Drop and join.  
Teaches: ThreadPool with workers and an mpsc channel; Job = Box<dyn FnOnce() + Send + 'static>; sharing the receiver via Arc<Mutex<Receiver>> (E0382); graceful shutdown with Drop, Option::take, and join; worker lock-scope releasing the guard before running a job

**L75 · Lesson 36 — Automated Tests** — `lessons/36-automated-tests.md`  
It teaches writing automated tests that pass when they don't panic, using #[test] functions, assertion macros, and #[should_panic], run by cargo test.  
Teaches: #[test] and cargo test runner; assert!, assert_eq!, assert_ne! (a test passes if it doesn't panic); #[should_panic]; #[cfg(test)] mod tests with use super::*;; reading a failing test's left/right output

**L76 · Lesson 37 — Documenting Rust: doc comments & doc-tests** — `lessons/37-doc-comments.md`  
It teaches Markdown doc comments (/// and //!) that cargo doc renders into an HTML manual, and doc-tests — code examples that cargo test compiles and runs so docs can't drift from code.  
Teaches: /// documents the item below; //! documents the enclosing item; Markdown doc comments and cargo doc --open; doc-tests: fenced examples run by cargo test; # Examples sections as part of the test suite; a lying doc example fails with left/right mismatch

**L77 · Lesson 37b — Shipping with Cargo: profiles, publishing & workspaces** — `lessons/37b-shipping-with-cargo.md`  
It surveys Cargo's shipping toolkit: release vs dev build profiles, publishing crates to crates.io with required metadata, workspaces of related crates, and cargo install.  
Teaches: dev vs release profiles ([profile.dev]/[profile.release], opt-level); cargo publish with required [package] description and license (yank is permanent, no delete); workspaces ([workspace] members, shared Cargo.lock and target/); cargo install for binary crates; measuring speed with --release, not the dev build

---


## 6 · How to regenerate

Run `python3 scripts/build-curriculum-map.py` from the repo root. It re-reads the lesson files, the `CONCEPT_LESSON` map, the exercise tomls, and `docs/curriculum-subjects.json`, and rewrites this file. To refine a lesson's subject/teaches, edit the JSON and re-run.

